export const metadata = {
  title: 'Dashboard - Best AI Tools Free',
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
